{
    "builds":[{
        "src": "dashboard/wsgi.py",
        "use": "@now/node-server",
        "config": { "maxLambdaSize": "10mb", "runtime" : "python3.9" }
    }],
    "routes": [{ 
        "src": "/(.*)", 
        "dest": "dashboard/wsgi.py" 
    }]
}